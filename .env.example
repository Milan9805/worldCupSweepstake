# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001
IS_LOCAL=true
TABLE_PREFIX=
AVATAR_BUCKET=sweepstake-avatars
PORT=3001

# Secrets (local dev only — in AWS these come from SSM Parameter Store)
# Setting these env vars bypasses SSM and is required for local development.
JWT_SECRET=dev-secret-change-in-production
FOOTBALL_DATA_API_KEY=your-api-key-here
